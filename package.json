{
  "name": "verde",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "bun run dev:all",
    "dev:all": "bun run --parallel dev:client dev:server",
    "dev:client": "bun run --cwd client dev",
    "dev:server": "bun run --cwd server start",
    "build": "bun run build:all",
    "build:all": "bun run build:client",
    "build:client": "bun run --cwd client build"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.4.1"
  }
}